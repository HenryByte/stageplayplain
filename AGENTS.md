# Agent Instructions

## Testing

- Write tests in BDD style: mark the phases of each test with `# Given`, `# When`, and `# Then` comments.
- Keep those comments concise — a short phrase stating what's being set up, done, or asserted, not a full explanation.
- Don't add other comments to tests beyond the Given/When/Then markers unless something is genuinely non-obvious.
